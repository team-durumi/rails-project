{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Create project",
            "type": "shell",
            "command": "rails new ./ -G --skip -d postgresql && cd project && cp ../.vscode/database.yml config && rake db:create"
        },
    ],
}